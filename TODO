= TODO

== nearest version

 * write Changes


== next versions

 * minimize running external programms. Just once run `git status --porcelain`
   and parce the output
 * add command line parameter --ignore_missing_paths
